@page
@model FTLSV2.Pages.Chairman.RegisterTeacherModel
@{
    Layout = "_ChairmanLayout";
}

<div class="content-card">
    <h2>Register Teacher</h2>

    @if (!string.IsNullOrEmpty(Model.Message))
    {
        <div style="margin-bottom:15px;color:#2d6a4f;font-weight:600;">
            @Model.Message
        </div>
    }

    <form method="post">
        <div class="form-group">
            <label>Faculty ID (8 digits)</label>
            <input asp-for="FacultyId" class="form-control" placeholder="12345678" maxlength="8" required />
        </div>

        <div class="form-group">
            <label>First Name</label>
            <input asp-for="FirstName" class="form-control" required />
        </div>

        <div class="form-group">
            <label>Last Name</label>
            <input asp-for="LastName" class="form-control" required />
        </div>

        <div class="form-group">
            <label>Email</label>
            <input asp-for="Email" type="email" class="form-control" required />
        </div>

        <div class="form-group">
            <label>Password</label>
            <input asp-for="Password" type="password" class="form-control" required />
        </div>

        <div class="form-group">
            <label>Confirm Password</label>
            <input asp-for="ConfirmPassword"
                   type="password"
                   class="form-control"
                   required />
        </div>

        <button type="submit" class="btn-primary">
            Register Teacher
        </button>
    </form>
</div>